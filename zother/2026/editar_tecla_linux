## 🛠️ Passo a passo (Debian)

### 1️⃣ Editar o layout brasileiro

```bash
sudo nano /usr/share/X11/xkb/symbols/br
```

Procure pelo bloco parecido com este:

```xkb
key <AE06> { [ 6, diaeresis, notsign, notsign ] };
```

---

### 2️⃣ Substitua por:

```xkb
key <AE06> { [ 6, diaeresis, bar, brokenbar ] };
```


### 3️⃣ Recarregar o layout

```bash
setxkbmap br
```

---

## ✅ Teste rápido

Abra qualquer editor e teste:

* `AltGr + 6` → `|`
* `Shift + AltGr + 6` → `¦`
