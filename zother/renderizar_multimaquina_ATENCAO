😂🛗🏁 Fechou, Zema! Agora o Bioquanti está oficialmente **“escada rolante–proof”**.

Só pra deixar o “mantra de bolso” registrado (pra nunca dar susto):

1. **Antes de mexer em qualquer máquina**

```bash
git pull --no-rebase origin main
```

2. **Depois de renderizar**

```bash
git add -A
git commit -m "Atualiza Bioquanti: ..."
git push
```

E com o `.Rproj` ignorado, a aba Git fica limpinha, só com o que importa.

Quando você quiser, a gente monta um **atalho único** tipo `bioquanti-sync` que faz pull + checa status + push (com mensagens de erro amigáveis) — perfeito pra modo “shopping center” 😄
